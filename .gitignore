# ============================================================
# Quantitative Trading — 根層 .gitignore
# 原則：可由程式重建 / 網上重抓 / 本地生成的檔案，一律不入 git
# ============================================================

# ---------- OS ----------
.DS_Store
Thumbs.db

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ---------- Virtual Environment（pip/uv 可重建）----------
.venv/
env/
venv/
ENV/

# ---------- 環境變數 / 密鑰（安全，切勿入 git）----------
.env
*.key
*.pem
credentials.json

# ---------- IDE ----------
.idea/
.vscode/
*.swp
*.swo

# ---------- 測試快取 / 覆蓋率 ----------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/

# ---------- 資料庫（可由 backfill 腳本重建，量大）----------
# taiwan-quant/data/history.db（~1GB）、qlib-tw-trader/data/data.db（~47MB）
*.db
*.db-shm
*.db-wal
*.sqlite3
*.db.bak*
*.db.backup*
# 跨機器搬運用的壓縮副本（~280MB）。`*.db` 不會匹配到 .gz 結尾
*.db.gz
*.db.zst
*.db.gz.sha256

# ---------- 訓練模型 / Qlib 匯出（可重訓 / 重匯出）----------
data/models/
data/qlib/
**/data/models/
**/data/qlib/

# ---------- Universe 快照（可重生）----------
**/data/universe_history/

# ---------- 日誌 ----------
*.log

# ---------- Frontend（npm install 重建）----------
node_modules/
frontend/dist/
*.tsbuildinfo

# ---------- Script 輸出 ----------
**/scripts/output/

# ---------- 工具設定（本地個人設定，不入 git）----------
.claude/

# ---------- DVC（個人雲端設定）----------
.dvc/
.dvcignore
**/data/*.dvc

# 保留：.planning/、docs/、reports/、所有原始碼、README、pyproject、requirements

# ---------- 外部參考專案（clone 來研究用，不納入版控）----------
# 前面的斜線是必要的：沒有它會連 taiwan_quant/validation/external/ 一起擋掉，
# 而那裡放的是要納入版控的 vendored 程式碼
/external/

# hook 產生的暫存檔。`.pending-auth-captures.jsonl` 可能含憑證片段，
# 絕不提交。`.planning/HANDOFF.json` 是刻意追蹤的，不受這條影響。
#
# 必須用 `**/` 前綴：含斜線的樣式會被錨定在本檔所在目錄，
# `.planning/.pending-*` 只會匹配 repo 根目錄那一層。
**/.planning/.pending-*
